import { ReactElement } from 'react';
import { Typography } from '@mui/material';

const SubtitleBlock = (): ReactElement => {
  return <Typography variant="h5">Subtitle</Typography>;
};

export default SubtitleBlock;
